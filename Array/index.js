let arr=[10,20,30,40,50,60,70];
console.log(arr.length);
document.getElementById("A").innerHTML=`the array element is: ${arr}`;
console.log(typeof arr);

const str=["rajib","dash",56,];
let i=0;
while(i<str.length)
{
    console.log(str[i]);
    i++;
}
